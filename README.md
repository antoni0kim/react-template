# React Template

This repo includes all the necessary dependencies for creating React app in TypeScript, including testing setup such as vitest + react testing library, and playwright. The following are in development to improve this template:

- Integrate kanban board with GitHub Project
- Add dependabot to automatically update dependencies
